@inherits Fluxor.Blazor.Web.Components.FluxorComponent

<div class="bstudio_nuget-package-display">
    
    <div>
        @NugetPackageRecord.Title
        @NugetPackageRecord.TotalDownloads
        
        <div style="margin-left: 2ch; padding-left: 5px; border-left: 1px solid var(--bstudio_primary-border-color)">
            <select @onchange="SelectedNugetVersionChanged">
                @{
                    var versions = NugetPackageRecord.Versions;
                    
                    foreach (var version in versions)
                    {
                        <option value="@version.Version"
                                selected="@(_nugetPackageVersionString == version.Version)">
                            @version.Version
                        </option>
                    }
                }
            </select>
            
            <ButtonDisplay OnClickFunc="AddNugetPackageReferenceOnClick"
                           CssStyleString="display: inline-flex; margin-left: 5px;"
                           IsDisabled="!ValidateSolutionContainsSelectedProject()">
                Add to @(NuGetPackageManagerStateWrap.Value.SelectedProjectToModify?.Name ?? "{project not yet selected}")
            </ButtonDisplay>
        </div>
    </div>
</div>